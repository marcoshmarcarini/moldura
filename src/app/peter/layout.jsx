export default function PeterLayout({ children }) {
    return (
        <html lang="pt-br">
            <body style={{overflow: 'hidden'}}>
                {children}
            </body>
        </html>
    )
}